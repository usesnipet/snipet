# Copy to web/.env. Used only by vite.config.ts (dev server), never bundled.

# Backend the /api proxy forwards to during `pnpm dev`.
BACKEND_URL=http://localhost:8080

# Injected as an HTTP Basic Authorization header on proxied /api requests.
BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=change-me-in-production
